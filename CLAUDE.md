# wafer_stage — 카메라 센싱 + 스테이지 제어

이 폴더는 "샘플 자동 측정 시스템"의 ① 센싱·모터 제어 부분이다. ② 실제 측정(계측기 제어)은 옆 폴더 measure\ 에 별도로 만든다(아직 없음). 촬영 결과·캘리브레이션 파일은 ..\data\ 에 두고 git 에 넣지 않는다.

## 목적
4인치 웨이퍼(또는 철판) 위 샘플 조각 10~16개를 카메라로 검출하고, XYZ 갠트리가 프로브를 각 샘플 위로 옮긴다.
정확도 요구: 프로브가 샘플 위 ±1~2mm 안에 오면 됨.

## 하드웨어 (실측 확정값)
- 컨트롤러: Arduino Mega 2560 + 자작 펌웨어 V6 (firmware/stage_v6). USB 시리얼 115200, 줄끝 \n
- 드라이버: 모터뱅크 MSD-224 ×3 (2상 스테핑, 1/8분주 = 1600펄스/회전). ENA LOW = 동작
- 축: X = 모터 2개 동기 구동(갠트리 양측, 논리축 하나) / Y = 빔 위 캐리지 / Z = 미장착(캐리지에 달릴 예정)
- 핀: X1 PUL2 DIR31 ENA30 / X2 PUL3 DIR49 ENA48 / Y PUL4 DIR53 ENA52
- 스케일: 160 펄스 = 1mm (X·Y 동일). 스트로크: X 0~39620 펄스(247.6mm), Y 0~39640(247.8mm)
- 좌표계: 각 축 모터 쪽 끝(하드스톱 2mm 이격) = 0. X+ = X모터 반대쪽, Y+ = Y모터 반대쪽
- 운전 설정: v6000 pps / a40000 / b300. 무부하 검증값 — Z 장착 후 재확인
- 리밋 스위치 없음(사용자 결정) → 소프트 리밋 + EEPROM 위치 기억
- 카메라: USB 인덱스 1, 1280×720 YUY2(MJPG 협상 실패), 작업 영역 위 수직 고정, 약 1.7 px/mm
- 베이스 기준 마커: ArUco DICT_4X4_50 30mm, id0~id3 시트를 베이스에 부착. 마커 중심 기계좌표(mm, 드라이버 포인터로 실측):
  id3 (15,15) / id2 (15,156) / id1 (201,15) / id0 (201,156)
- 캐리지 앞에 드라이버 포인터 — 육안 위치 확인용

## 아두이노 프로토콜 (V6)
- 원점: fz x [탐색펄스] / fz y [탐색펄스] (−끝까지 밀고 2mm 이격 후 0) / z zx zy (현재 위치=0) / sp <x> <y> (PC가 위치 지정) / home
- 절대 이동: gx <펄스> gy <펄스> g <x> <y> / mx <mm> my <mm>
- 상대 이동: x <펄스> y <펄스>
- 상태: p (사람용) / st → "ST X=<펄스> Y=<펄스> X2=<펄스> HX=0|1 HY=0|1 DIRTY=0|1"
- 저장: save / forget      기타: v a b w / e 0|1 / ! (이동 중 비상정지)
- 이동 완료 보고 줄: "  X  + cmd=16000 out=16000 t=2783.53ms | X=16000 Y=8000" — "| X=" 이후 파싱
- 거부 줄: "  [거부] 범위 밖 ..." / "  [거부] 원점 미확정 ..."
- 규칙: 원점 없으면(HX 또는 HY=0) fz 외 모든 이동 거부. 이동은 블로킹. 부팅 시 EEPROM 복원하되 save 없이 껐으면 원점없음. e 1 하면 원점 해제.

## 파일
- main.py: Tkinter UI, 단발 촬영 → 검출 → 저장
- camera.py: 캡처. capture_best() 로 여러 프레임 중 선명한 것
- detect.py: 샘플 검출. 파이프라인 고정(아래). 수정 금지
- report.py: DATA_DIR/out/<타임스탬프>/ 에 raw·annotated 저장 + log.jsonl 한 줄
- calib.py: 카메라↔기계좌표 캘리브레이션 (fit / px2mm / mm2px / samples)
- paths.py: PROJECT_ROOT, DATA_DIR(settings "data_dir", 기본 ../data), OUT_DIR, LOG_PATH, CALIB_PATH, SETTINGS_PATH
- settings.json: 사용자가 직접 편집. GUI 슬라이더 없음
- firmware/stage_v6/stage_v6.ino: 아두이노 펌웨어

## 검출 파이프라인 (검증 완료·고정)
무채색 마스크(S<45,V>60) → OPEN → 최대 연결요소 → 구멍 메우기 → CLOSE → 광선 스캔 → fitEllipse.
타원 장축=스케일, 단축비=기울기. 중앙값 배경 나눗셈. 히스테리시스 15%/12% + 채도 OR. 면적 필터 mm².
실패해서 반복 금지: CLOSE 먼저 / 배경 블러 커널=샘플 크기 / Otsu / top-hat·black-hat / 꼭짓점 4개로 사각형만 거르기 / 화면 밖 광선 끝점 / 이미지 위 한글 / 오버레이 패널 크기 상수
surface 모드 wafer/plate/auto. plate 는 ArUco 로 스케일. 웨이퍼는 은색 무광·초록 코팅 등 여러 종류.

## 캘리브레이션 (calib.py)
- fit [--image]: 기준 마커 검출 → 픽셀→기계(mm) 어파인(estimateAffine2D), 4점이면 호모그래피도 → DATA_DIR/calib_matrix.json
- 현재: id0,1,3 세 점으로 fit 됨(잔차 무의미). id2 미검출 원인 조사 중.
- 전제: 카메라 위치·각도·초점 고정. 움직이면 fit 재실행.

## 최종 동작 시퀀스 (목표)
1. st 로 원점 확인 → 없으면 fz x / fz y (또는 파일 위치로 sp)
2. home — 갠트리를 카메라 시야 밖으로
3. 촬영 + 검출 → 샘플 중심 픽셀
4. calib_matrix 로 픽셀 → 기계 mm. 0~247 밖이면 제외 + 경고
5. 샘플마다: [Z 상승 확인] → mx / my → 완료 보고 대기 → [Z 하강 → 측정(measure\ 담당) → Z 상승]
6. home → save
- Z·측정은 미정(Z 모터 미도착, 프로브 종류 미정). 인터페이스(함수)만 비워둔다.
- 백래시 무시 가능(실측). 접근 방향 통일 현재 불필요.

## 안전
- 프로브 장착 후 충돌 = 프로브 파손. 소프트 리밋이 유일한 방어선 → 원점 정확도가 전부.
- 전원 끄기 전 save. 전원 꺼진 동안 손으로 밀지 않는다.
- fz 탐색 거리는 넉넉하게. 하드스톱 충돌은 시리얼로 감지 불가(보고는 정상으로 나옴).

## 다음 구현 목표 (순서대로)
1. stage.py — 시리얼 드라이버: 연결 → st 파싱 → 필요시 sp → mx/my 전송 → 완료/거부 줄 파싱 → save. 이동 전후 위치를 DATA_DIR/stage_state.json 에 기록(이동 전 moving=true, 보고 받으면 위치+moving=false).
2. run_sequence.py — 위 시퀀스를 Z 없이 XY 까지. 샘플마다 사용자 Enter 대기 옵션.
3. 카메라 이동 감지 — 세션 시작 시 기준 마커 픽셀이 calib 당시와 N px 이상 다르면 경고.
4. (Z 도착 후) Z 축 추가, 속도·가속도 재검증, 리밋 스위치 재검토.

## 작업 규칙
- 하드웨어 실행은 사용자가 한 단계씩. 코드는 요청 범위만, 한 번에 여러 기능 넣지 않기.
- Tkinter 전용, cv2.imshow 절대 금지, exe/bat 안 만듦, 오프라인 재현 --image 필수.
- 특정 사진에 오버피팅 금지 — 지금까지 찍은 사진 전체로 회귀 검증.
- md 문서 만들지 않음(README·CLAUDE.md 제외). 프롬프트 사본·작업 기록 생성 금지.
- 검출 로직 수정 금지. 땜질 대신 근본적 해결.
- 상세 기록은 Obsidian 01_Projects/Auto_Measurement_Program 에 사용자가 관리.